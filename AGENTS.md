# Nanika Box 작업 지침

이 프로젝트의 사양 입구는 [docs/index.md](docs/index.md)다. [docs/PRODUCT.md](docs/PRODUCT.md)는 기존 0.2 기준을 보존하며, 생활 도구·외부 위젯의 확장 계약은 `docs/product/`와 `docs/widgets/`에서 관리한다. 기능·화면·대화 흐름을 변경하기 전에 해당 항목과 인수 기준을 읽고, [docs/status.md](docs/status.md)에서 구현과 계획을 구분한다. 최신 사용자 지시와 상위 지침을 우선하며, 기존 제품 결정과 충돌하는 변경은 그 차이를 분명히 설명한다.

- 중심 경험은 **바탕화면에 상주하는 작은 A/B 본체와 잠깐 나타났다 사라지는 말풍선**이다. 대화 기록·설정·친밀도는 보조 화면에 둔다. 주 화면을 큰 LLM 채팅 패널이나 대시보드로 다시 구성하지 않는다.
- 사용자 입력·모델 다운로드·API 설정 없이도 내장 인사와 자동 잡담이 동작해야 한다. LLM은 선택 기능이며 기본 경험의 준비 조건이 아니다.
- 단어장은 모델 준비 검사보다 먼저 매칭한다. 활성 항목의 포함 키워드 중 긴 키워드, 동률이면 최초 등록 항목을 사용한다. 등록 본문·공백·줄바꿈·화자 순서를 그대로 재생한다.
- 자동 생성과 자동 재생의 중단 조건을 함께 검토한다. 사용자 입력·숨김·일시정지·상위 자동 설정·재시작 이후 stale 작업이 다시 표시되지 않게 한다. 준비 실패를 빠른 재시도로 처리하지 않는다.
- 설정·모델 전환·단어장 변경 때문에 원문 기록·기억·친밀도를 초기화하지 않는다. 기본 예제는 한 번만 등록하고 삭제한 항목을 복원하지 않는다.
- 기존 `action`·`gate`·epoch·revision 경계를 확인한다. 상태 변경과 저장은 최신 작업에만 허용하고 표준 mutex guard를 `await` 너머로 유지하지 않는다. 앱이 소유한 추론 프로세스만 관리한다.
- 구현 범위를 요청한 파일과 동작으로 제한한다. 기존 도구와 함수 중심 구조를 우선한다. 재생·취소·키워드 우선순위·데이터 보존 변경은 해당 책임의 회귀 테스트로 확인한다.
- 완료 보고에서는 사양, 코드 검증, 실제 데스크톱 검증, 모델 의미 품질을 구분한다. 테스트 개수·속도·메모리·지원 플랫폼의 실측 결과를 이전 빌드에서 가져와 현재 결과처럼 제시하지 않는다.

명령과 환경 준비는 [README.md](README.md)를 따른다. 제품 계약을 바꾸는 승인된 작업이면 해당 주제 문서와 `docs/status.md`를 함께 갱신한다. 기존 0.2 동작이 바뀌면 `docs/PRODUCT.md`의 관계도 설명한다. Docusaurus는 `docs/`를 직접 읽으므로 위키용 사양 사본을 만들지 않는다. 위키 실행·검증은 [docs/development/wiki.md](docs/development/wiki.md)를 따른다.
