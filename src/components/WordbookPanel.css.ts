import { style } from "@vanilla-extract/css";
export const entries = style({ display: "flex", flexWrap: "wrap", gap: 8, margin: "14px 0" });
export const editor = style({ border: 0, margin: 0, padding: 0, minWidth: 0 });
export const legend = style({ fontSize: 12, color: "#6b7269", marginBottom: 12 });
export const line = style({ marginTop: 14, paddingTop: 12, borderTop: "1px solid #eceee4" });
export const actions = style({ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 16 });
